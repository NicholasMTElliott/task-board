# Local environment files
.env.local
.env
.env.*
!.env.example
**/.env.local
**/.env
**/.env.*
!**/.env.example

# User-specific config (secrets — replaces .env.local for deployed exe)
**/appsettings.user.json
.vs/

# Generated local Worker secret bindings
worker/.dev.vars
worker/.env
worker/.env.*
**/.dev.vars
**/.dev.vars.*

# Agent workspace artifacts (created by integration tests / agent runs)
.aiboard/

# Generated build artifacts
**/bin/
**/obj/
**/node_modules/
**/dist/
**/coverage/
**/.wrangler/
**/*.tsbuildinfo

# Claude Code per-project state (settings.local.json, scheduled_tasks.lock,
# worktrees/, etc.) — generated locally by the assistant; not source-of-truth.
.claude/
